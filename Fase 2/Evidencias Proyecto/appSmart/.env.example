# Configuración de ejemplo para SmartERP
# Copiar a .env.development o .env.production y ajustar valores

# Django Core
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=django-insecure-CAMBIAR-EN-PRODUCCION
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Base de Datos SQL Server
DB_ENGINE=mssql
DB_NAME=inventario
DB_HOST=AOANBC02CW0729\SQLEXPRESS
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_TRUSTED_CONNECTION=yes
DB_TRUST_SERVER_CERTIFICATE=yes

# Fallback automático a SQLite (solo desarrollo)
# Si SQL Server no está disponible, automáticamente usa SQLite local
USE_SQLITE_FALLBACK=True

# Alternativamente, forzar SQLite para desarrollo sin SQL Server:
# DB_ENGINE=sqlite3

# Seguridad (solo para producción)
# DJANGO_CSRF_TRUSTED_ORIGINS=https://tudominio.com

# Cache (opcional)
# REDIS_URL=redis://localhost:6379/0

# Logging level
# LOG_LEVEL=INFO